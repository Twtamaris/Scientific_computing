// print statement in cpp
#include <iostream>
using namespace std;
// Define function to add two numbers
int add(int a, int b)
{
    return a + b;
}

int main()
{
    cout << "Hello World!" << endl;
    cout << "Hello World" << endl;
    //take input from user
    int a;
    cout << "Enter a number: ";
    cout << "Enter a number: ";
    cin >> a;
    cout << "You entered: " << a << endl;
    cout << "You pressed: " << a << endl;
    // while loop in cpp
    int i = 0;
    while (i < 10)
    {
        cout << i << endl;
        i++;
    }
    // while loop for infinity loop
    
    // for loop in cpp
    for (int i = 0; i < 10; i++)
    {
        cout << i << endl;
    }
    for (int i=0; i<100; i = i+2){
        cout << i << endl;
    }
    // using funcitons in cpp
    add(10, 20);
    cout << add(10, 20) << endl;
    // This is Awesome
    
}
